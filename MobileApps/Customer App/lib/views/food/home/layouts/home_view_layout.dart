import 'package:flutter/material.dart';

class FoodHomeViewLayout extends StatelessWidget {
  const FoodHomeViewLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Food Home view'),
      ),
    );
  }
}
