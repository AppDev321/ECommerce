import 'package:flutter/material.dart';

class FoodDashboardLayout extends StatelessWidget {
  const FoodDashboardLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Food Dashboard Laoyout'),
      ),
    );
  }
}
