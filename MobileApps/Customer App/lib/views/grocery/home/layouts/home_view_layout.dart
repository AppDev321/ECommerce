import 'package:flutter/material.dart';

class GroceryHomeViewLayout extends StatelessWidget {
  const GroceryHomeViewLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Grocery Home view'),
      ),
    );
  }
}
