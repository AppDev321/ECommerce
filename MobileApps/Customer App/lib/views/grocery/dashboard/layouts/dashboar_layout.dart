import 'package:flutter/material.dart';

class GroceryyDashboardLayout extends StatelessWidget {
  const GroceryyDashboardLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Grocary Dashboard Laoyout'),
      ),
    );
  }
}
