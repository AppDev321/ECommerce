import 'package:flutter/material.dart';

class GroceryDashboardLayout extends StatelessWidget {
  const GroceryDashboardLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pharmacy Dashboard Laoyout'),
      ),
    );
  }
}
